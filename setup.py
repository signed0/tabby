#!/usr/bin/env python

import os.path
from setuptools import setup

setup(
    name = 'Tabby',
    packages = [],
    version = '0.1',

    author = 'Nathan Villaescusa',
    author_email = 'nathan@signedzero.com',
    description = 'Reads tabular data and turns it into python objects',
    keywords = 'csv tsv',
    url = 'http://github.com/signed0/tabby',
    license = 'BSD',
    entry_points = {},
    classifiers = [],
    install_requires = [],
)